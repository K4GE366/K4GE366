<!DOCTYPE html>
<html>
    <head>
        <title>Page Title</title>
    </head>
    <body>
        
    </body>
</html><!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>🎀 Hello Kitty Calculator 🎀</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Comic+Neue&display=swap');

    body {
      margin: 0;
      font-family: 'Comic Neue', cursive;
      background: radial-gradient(#ffe6f0, #ffc6e6);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: #d63384;
      overflow: hidden;
    }

    body::before {
      content: "💖";
      font-size: 100px;
      position: absolute;
      top: 10%;
      left: 10%;
      opacity: 0.05;
      animation: float 5s infinite ease-in-out;
    }

    body::after {
      content: "🎀";
      font-size: 100px;
      position: absolute;
      bottom: 10%;
      right: 10%;
      opacity: 0.05;
      animation: float 6s infinite ease-in-out reverse;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    .calculator {
      background: #fff0f5;
      border-radius: 30px;
      box-shadow: 0 0 25px #ff80c0;
      padding: 20px;
      width: 360px;
      border: 3px dashed #ffb6c1;
      position: relative;
      text-align: center;
    }

    .kitty-img {
      width: 120px;
      margin-bottom: 10px;
      border-radius: 20px;
    }

    .title {
      font-size: 1.6rem;
      margin-bottom: 10px;
      color: #ff4da6;
    }

    input {
      width: 100%;
      height: 60px;
      font-size: 1.4rem;
      padding: 10px;
      margin-bottom: 20px;
      border: none;
      background: #ffe6f0;
      color: #c71585;
      border-radius: 15px;
      text-align: right;
      box-shadow: inset 0 0 5px #ffb6c1;
    }

    .buttons {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 10px;
    }

    button {
      padding: 15px;
      font-size: 1.1rem;
      border: none;
      border-radius: 15px;
      background: #ffb6c1;
      color: white;
      cursor: pointer;
      transition: 0.2s;
      box-shadow: 0 4px #e75480;
    }

    button:active {
      box-shadow: none;
      transform: translateY(3px);
    }

    .wide {
      grid-column: span 2;
    }
  </style>
</head>
<body>
  <div class="calculator">
    
    <div class="title">🎀 Hello Kitty Calculator 🎀</div>
    
    <img 
src="https://www.citypng.com/photo/2dc87236/lovely-hello-kitty-holding-strawberry-transparent-background" class="kitty-img" alt="Hello Kitty with Strawberry">
    <input type="text" id="display" disabled placeholder="Start calculating, cutie~ 🐱" />
    <div class="buttons">
      <button onclick="clearDisplay()">C</button>
      <button onclick="addToDisplay('π')">π</button>
      <button onclick="addToDisplay('^')">^</button>
      <button onclick="addToDisplay('/')">➗</button>

      <button onclick="addToDisplay('sin(')">sin</button>
      <button onclick="addToDisplay('cos(')">cos</button>
      <button onclick="addToDisplay('tan(')">tan</button>
      <button onclick="addToDisplay('*')">✖️</button>

      <button onclick="addToDisplay('7')">7</button>
      <button onclick="addToDisplay('8')">8</button>
      <button onclick="addToDisplay('9')">9</button>
      <button onclick="addToDisplay('-')">➖</button>

      <button onclick="addToDisplay('4')">4</button>
      <button onclick="addToDisplay('5')">5</button>
      <button onclick="addToDisplay('6')">6</button>
      <button onclick="addToDisplay('+')">➕</button>

      <button onclick="addToDisplay('1')">1</button>
      <button onclick="addToDisplay('2')">2</button>
      <button onclick="addToDisplay('3')">3</button>
      <button onclick="addToDisplay('√(')">√</button>

      <button onclick="addToDisplay('0')">0</button>
      <button onclick="addToDisplay('.')">.</button>
      <button class="wide" onclick="calculate()">=</button>
    </div>
  </div>

  <script>
    const display = document.getElementById("display");

    function addToDisplay(value) {
      if (value === 'π') {
        display.value += Math.PI.toFixed(8);
      } else {
        display.value += value;
      }
    }

    function clearDisplay() {
      display.value = "";
    }

    function calculate() {
      try {
        let expression = display.value
          .replace(/√/g, "Math.sqrt")
          .replace(/sin\(/g, "Math.sin(Math.PI/180*")
          .replace(/cos\(/g, "Math.cos(Math.PI/180*")
          .replace(/tan\(/g, "Math.tan(Math.PI/180*")
          .replace(/log\(/g, "Math.log10(")
          .replace(/\^/g, "**");

        display.value = eval(expression);
      } catch {
        display.value = "💥 Error 💥";
      }
    }
  </script>
</body>
</html>